1)
#include <stdio.h>

int main(){
    int idade, categoria;
    printf("Digite a idade do nadador:");
    scanf("%d", &idade);
    
    if(idade >= 5 && idade <= 7){
    printf("Seu nadador pertence a categoria Infantil A");
    }else if(idade >= 8 && idade <= 10 ){
        printf("Seu nadador pertence a categoria Infantil B");
    }else if(idade >= 11 && idade <= 13){
        printf("Seu nadador pertence a categoria Juvenil A");
    }else if(idade >= 14 && idade <= 17){
        printf("Seu nadador pertence a categoria Juvenil B");
    }else{
        printf("Seu nadador pertence a categoria Adulto");
    }
        
    }
2)
#include <stdio.h>
#include <stdbool.h>

int main()
{
    int a;
    bool resultado;
    
    printf("Digite um valor a ser dado a incogna a:");
    scanf("%d", &a);
    
    if(a % 2 == 0){
        
        resultado = true;
        
    } else{
        
        resultado = false;
    }
    
    printf("Resultado: %s\n", resultado ? "true" : "false");
    
    return 0;
    
    
}
3)
#include <stdio.h>
#include <stdbool.h>

int main()
{
    
    float N;
    float S;
    
    printf("Digite um valor inteiro e positivo para a incognita N:"); 
    scanf("%f", &N);
    
    S = 1 + (1/2) + (1/3) + (1/4) + (1/5) + (1/N);
    
    printf("O valor de S é %f", S);
    
}
4)
#include <stdio.h>
#include <stdbool.h>

int main()
{
    float alt;
    float peso_idealM;
    float peso_idealH;
    
    printf("digite a sua altura:");
    scanf("%f", &alt);
    
    peso_idealH = (72.2 * alt) - 58;
    peso_idealM = (62.1 * alt) - 44.7;
    
    printf("O peso ideal para um homem com essa altura é:%f, e para uma mulher é:%f", peso_idealH, peso_idealM);
     
    
}
